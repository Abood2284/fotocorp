import { PlaceholderPage } from "@/components/layout/placeholder-page"

export const metadata = {
  title: "Services — Fotocorp",
}

export default function ServicesPage() {
  return (
    <PlaceholderPage
      eyebrow="Services"
      title="Archive research, event coverage, and image licensing support."
      description="Service workflows will be expanded here. For now, the public archive remains the primary discovery surface."
    />
  )
}
