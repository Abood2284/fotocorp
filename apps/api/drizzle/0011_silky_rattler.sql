-- Snapshot bridge generated before DB Revamp PR-01.
-- The schema changes represented by this snapshot already exist in earlier
-- migration SQL files and on the Neon Development branch. Keep this migration
-- as an idempotent no-op so Drizzle can advance from the regenerated snapshot
-- history without recreating existing tables.
SELECT 1;
